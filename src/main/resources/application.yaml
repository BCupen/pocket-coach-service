spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pocket_coach
    username: brittneycupen
    password: Password1
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
